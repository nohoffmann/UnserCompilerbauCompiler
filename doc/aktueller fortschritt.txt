aktueller Fortschritt:
 - math. Terme mit Grundrechenarten (bisher nur ganzzahlig) koennen übersetzt werden
 - Grammatik für Variablen/Konstanten-Deklarationen/Definitionen/Zuweisungen scheint grundlegende Funktionalität zu besitzen
 - Grammatik für logischee Ausdruecken (Boole'sche Algebra) inkl. Klammerung formuliert (AND, OR sowie NOT implementiert)
 - TestNG als Test-Framework: Testfälle können in der Methode provide_code_expectedText() in der Datei Compiler/test/de/progbusters/compiler/CompilerTest.java hinzugefuegt werden. Wenn TestNG in Eclipse eingebunden ist, kann man von dort aus die Testfälle ausführen lassen.
 - Funktion println( [argument] ) implementiert. Bisher ist als "argument" ein arithmetischer Term möglich. Es ist möglich, mehrmals println in einer Quelldatei aufzurufen.
 
 - Variablen implementiert
 - Funktionsdefinitionen und -aufrufe grundsätzlich implementiert (TODO: u.a. besseres Exceptionhandling)

- if-else-statement implementiert, ähnlich wie in C wird mit int gearbeitet, da es keinen boolean datentyp gibt. 0 entspricht false, alles andere true.

- vergleichsoperatoren implementiert

- logische verknüogunfen implementiert


